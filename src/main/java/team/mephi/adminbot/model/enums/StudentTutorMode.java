package team.mephi.adminbot.model.enums;

/**
 * Режим назначения студента на тьютора.
 *
 * Определяет тип назначения:
 * - INITIAL - первичное назначение
 * - REASSIGN - повторное назначение (переназначение)
 */
public enum StudentTutorMode {
    INITIAL,    // Первичное назначение
    REASSIGN    // Переназначение (повторное назначение)
}

