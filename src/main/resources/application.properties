server.port=8080
spring.profiles.active=${SPRING_ACTIVE_PROFILE:dev}
# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/flexiq_db
spring.datasource.username=postgres
spring.datasource.password=mysecretpassword
# JPA
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Thymeleaf
spring.thymeleaf.enabled=true
spring.thymeleaf.cache=false
# Keycloak OAuth2 Resource Server Configuration
spring.security.oauth2.client.registration.keycloak.client-id=admin-bot-client
spring.security.oauth2.client.registration.keycloak.client-secret=zxEweBxQCpBH6B5Pfv37wEE6DH17mzdM
spring.security.oauth2.client.registration.keycloak.scope=openid,profile,email
spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.keycloak.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.provider.keycloak.authorization-uri=http://localhost:8081/realms/mephi-realm/protocol/openid-connect/auth
spring.security.oauth2.client.provider.keycloak.token-uri=http://localhost:8081/realms/mephi-realm/protocol/openid-connect/token
spring.security.oauth2.client.provider.keycloak.jwk-set-uri=http://localhost:8081/realms/mephi-realm/protocol/openid-connect/certs
#spring.security.oauth2.client.provider.keycloak.end-session-uri=http://localhost:8081/realms/mephi-realm/protocol/openid-connect/logout
#spring.security.oauth2.client.provider.keycloak.user-info-uri=http://localhost:8081/realms/mephi-realm/protocol/openid-connect/userinfo
#spring.security.oauth2.client.provider.keycloak.issuer-uri=http://localhost:8081/realms/mephi-realm
# Keycloak Realm and Client Configuration
keycloak.realm=mephi-realm
keycloak.client-id=admin-bot-client
keycloak.auth-server-url=http://localhost:8081

app.redirect-url=http://localhost:8080

# NeoStudy Integration Configuration
neostudy.enabled=true
neostudy.base-url=https://api.neostudy.example.com
neostudy.api-key=your-api-key-here
# Optional OAuth2 configuration (if using OAuth2 instead of API key)
# neostudy.token-url=https://api.neostudy.example.com/oauth/token
# neostudy.client-id=your-client-id
# neostudy.client-secret=your-client-secret
neostudy.timeout.connect=5s
neostudy.timeout.read=30s
neostudy.timeout.write=10s
neostudy.retry.max-attempts=3
neostudy.retry.backoff-delay=1s
neostudy.retry.multiplier=2.0
# Vaadin
vaadin.launch-browser=false
#vaadin.url-mapping=/v2/*
#vaadin.productionMode=true

server.servlet.session.persistent=false

#logging.level.org.springframework.transaction.interceptor=TRACE
#logging.level.org.springframework.orm.jpa.JpaTransactionManager=DEBUG

spring.servlet.multipart.enabled=false